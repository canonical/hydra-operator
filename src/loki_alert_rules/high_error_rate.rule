alert: HostHighLogErrorRate
expr: count_over_time({%%juju_topology%%} |= "error" [1m]) > 0
for: 1m
labels:
  severity: error
annotations:
  summary: High error rate in logs (instance {{ $labels.instance }})
  description: "High error rate in logs\n  VALUE = {{ $value }}\n  LABELS = {{ $labels }}"